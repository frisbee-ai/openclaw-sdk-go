---
phase: 03-client-struct-refactor
plan: '02'
type: execute
wave: '2'
depends_on:
  - '03-01'
files_modified:
  - pkg/client.go
autonomous: true
requirements:
  - API-02
must_haves:
  truths:
    - "Close() interface doc clearly states it shuts down entire client and releases all resources"
    - "Disconnect() interface doc clearly states it disconnects without shutting down client"
    - "User can understand the difference between Close() and Disconnect() from reading interface docs"
  artifacts:
    - path: pkg/client.go
      provides: "Close() and Disconnect() interface docs updated per D-03"
      contains: "Shuts down the entire client"
      contains: "without shutting down the client"
  key_links:
    - from: OpenClawClient interface
      to: Disconnect() method
      via: "updated interface docs explain semantics"
      pattern: "Disconnect.*closes the connection"
    - from: OpenClawClient interface
      to: Close() method
      via: "updated interface docs explain semantics"
      pattern: "Close.*shuts down"
---

<objective>
Update the OpenClawClient interface documentation for Close() and Disconnect() to clearly explain their distinct semantics. Per D-03: Close() = "Shuts down the entire client and releases all resources." Disconnect() = "Disconnects from the server without shutting down the client."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@pkg/client.go

# Current interface docs (lines 347-392):
# - Disconnect() line 350: "closes the connection gracefully" (vague, ambiguous)
# - Close() line 362: "shuts down the client and releases all resources" (clear but could be clearer)

This plan is purely documentation. No code logic changes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update interface docs for Close() and Disconnect()</name>
  <files>pkg/client.go</files>
  <read_first>pkg/client.go</read_first>
  <action>
    Update the OpenClawClient interface (around lines 347-392) with precise Close/Disconnect documentation:

    Line 350-351 — Change:
    ```go
    // Disconnect closes the connection gracefully.
    Disconnect() error
    ```
    To:
    ```go
    // Disconnect disconnects from the server without shutting down the client.
    // Stops reconnection attempts and cleans up connection state.
    // Call Connect() to reconnect.
    Disconnect() error
    ```

    Line 362-363 — Change:
    ```go
    // Close shuts down the client and releases all resources.
    Close() error
    ```
    To:
    ```go
    // Close shuts down the entire client and releases all resources.
    // No further operations are valid after calling Close.
    Close() error
    ```

    Keep all other interface methods unchanged. This plan is documentation-only — no code logic changes.
  </action>
  <verify>
    <automated>grep -A3 "func (c \*client) Disconnect" pkg/client.go | head -10</automated>
  </verify>
  <acceptance_criteria>
    - [ ] grep "Disconnect disconnects from the server without shutting down the client" returns non-empty
    - [ ] grep "Call Connect\(\) to reconnect" returns non-empty
    - [ ] grep "Shuts down the entire client and releases all resources" returns non-empty
    - [ ] grep "No further operations are valid after calling Close" returns non-empty
    - [ ] go build ./pkg/... succeeds (no logic changes)
  </acceptance_criteria>
  <done>Interface docs for Close() and Disconnect() clearly distinguish their semantics</done>
</task>

</tasks>

<verification>
1. `go build ./pkg/...` compiles without errors
2. Interface docs are human-readable and clear
</verification>

<success_criteria>
- Close() interface doc states "Shuts down the entire client and releases all resources. No further operations are valid after calling Close."
- Disconnect() interface doc states "Disconnects from the server without shutting down the client. Call Connect() to reconnect."
- User reading the interface can understand which to call for their use case
- go build ./pkg/... succeeds (documentation-only changes)
</success_criteria>

<output>
After completion, create `.planning/phases/03-client-struct-refactor/03-02-SUMMARY.md`
</output>
