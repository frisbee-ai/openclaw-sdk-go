---
phase: 05-release-infrastructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .goreleaser.yaml
autonomous: true
requirements:
  - REL-01

must_haves:
  truths:
    - "GoReleaser is configured for library mode with gomod.proxy enabled"
    - "GoReleaser uses mode: github to create GitHub Releases (not replace)"
    - "Release configuration includes GitHub owner and repo"
    - "before.hooks only runs go mod tidy (no redundant test or generate)"
  artifacts:
    - path: .goreleaser.yaml
      provides: GoReleaser v2 configuration for library distribution
      contains: "mode: github"
    - path: .goreleaser.yaml
      provides: GitHub release target
      contains: "github:"
  key_links:
    - from: .goreleaser.yaml
      to: github.com/frisbee-ai/openclaw-sdk-go
      via: release.github.owner and release.github.name
---

<objective>
Update .goreleaser.yaml to configure GoReleaser v2 for library mode with proper GitHub Release integration.

Purpose: Enable automated GitHub Releases via GoReleaser when version tags are pushed.
Output: Updated .goreleaser.yaml with mode: github, release section, and cleaned up hooks.
</objective>

<context>
@.goreleaser.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .goreleaser.yaml configuration</name>
  <files>.goreleaser.yaml</files>
  <action>
Update .goreleaser.yaml with the following changes per D-01, D-02, D-04, D-05:

1. Change `release.mode: replace` to `release.mode: github`
2. Add `release.github` section:
   ```yaml
   github:
     owner: frisbee-ai
     name: openclaw-sdk-go
   ```
3. Change `before.hooks` from:
   ```yaml
   hooks:
     - go mod tidy
     - go generate ./...
     - go test ./...
   ```
   To:
   ```yaml
   hooks:
     - go mod tidy
   ```
4. Set `release.draft: false` (already not draft, confirm)

The final .goreleaser.yaml should be:
```yaml
version: 2

project_name: openclaw-sdk-go

before:
  hooks:
    - go mod tidy

gomod:
  proxy: true
  mod: mod

builds:
  - id: go-build
    skip: true

snapshot:
  version_template: "{{ incpatch .Version }}-next"

release:
  github:
    owner: frisbee-ai
    name: openclaw-sdk-go
  mode: github
  prerelease: auto
  draft: false
```
</action>
  <read_first>.goreleaser.yaml</read_first>
  <acceptance_criteria>
    - File contains `mode: github` (not `mode: replace`)
    - File contains `github:` section with `owner: frisbee-ai` and `name: openclaw-sdk-go`
    - File contains `hooks:` with only `- go mod tidy` (no `go generate` or `go test`)
    - File contains `gomod:` section with `proxy: true`
    - File contains `builds:` with `skip: true`
    - File contains `draft: false`
  </acceptance_criteria>
  <verify>
    <automated>goreleaser check 2>&1 || echo "goreleaser not installed - will be validated in CI"</automated>
  </verify>
  <done>GoReleaser configuration updated with mode: github, release section, and cleaned up before.hooks</done>
</task>

</tasks>

<verification>
- goreleaser check validates YAML syntax and schema
- Config produces no errors when parsed by GoReleaser v2
</verification>

<success_criteria>
- .goreleaser.yaml passes `goreleaser check` with no errors
- Config contains all required library mode settings (proxy, skip builds, github mode)
</success_criteria>

<output>
After completion, create `.planning/phases/05-release-infrastructure/05-01-SUMMARY.md`
</output>
