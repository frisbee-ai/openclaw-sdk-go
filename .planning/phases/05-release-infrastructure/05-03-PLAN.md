---
phase: 05-release-infrastructure
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - REL-03

must_haves:
  truths:
    - "git-cliff is configured to parse conventional commits"
    - "git-cliff generates changelog with categorized sections"
    - "release.yml integrates git-cliff with GoReleaser"
    - "CHANGELOG.md exists with git-cliff footer"
  artifacts:
    - path: cliff.toml
      provides: git-cliff configuration for changelog generation
      contains: "conventional_commits = true"
    - path: .github/workflows/release.yml
      provides: Release workflow with git-cliff integration
      contains: "git-cliff"
    - path: CHANGELOG.md
      provides: Existing changelog generated by git-cliff
      contains: "generated by git-cliff"
  key_links:
    - from: .github/workflows/release.yml
      to: cliff.toml
      via: git-cliff --config cliff.toml
    - from: .github/workflows/release.yml
      to: goreleaser
      via: --release-notes /tmp/RELEASE_NOTES.md
---

<objective>
Verify git-cliff changelog automation is correctly configured (D-09, D-10, D-12).

Purpose: Confirm all changelog infrastructure is in place and working. The configs are already done per user decisions -- this is a verification task.
Output: Verified working git-cliff integration with release workflow.
</objective>

<context>
@cliff.toml
@.github/workflows/release.yml
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify git-cliff configuration</name>
  <files>cliff.toml</files>
  <action>
Verify git-cliff is correctly configured:

1. Verify cliff.toml has required settings:
   - conventional_commits = true
   - commit_parsers for feat, fix, perf, refactor, test, doc, build, ci, chore
   - changelog header and body templates
   - footer with "generated by git-cliff"

2. Test git-cliff locally:
   ```bash
   # Install if needed: go install github.com/orhun/git-cliff/cmd/git-cliff@latest
   git-cliff --config cliff.toml --verify
   ```

3. Verify CHANGELOG.md exists with git-cliff footer:
   ```bash
   grep "generated by git-cliff" CHANGELOG.md
   ```

4. Generate full changelog to verify output:
   ```bash
   git-cliff --config cliff.toml --strip all
   ```
   Should produce categorized sections (Features, Bug Fixes, Performance, etc.)
</action>
  <read_first>cliff.toml</read_first>
  <acceptance_criteria>
    - cliff.toml contains `conventional_commits = true`
    - cliff.toml contains commit_parsers for feat, fix, perf, refactor
    - cliff.toml contains footer "generated by git-cliff"
    - git-cliff --verify passes without error
    - git-cliff --strip all produces non-empty changelog output
    - CHANGELOG.md contains "generated by git-cliff" footer
  </acceptance_criteria>
  <verify>
    <automated>git-cliff --config cliff.toml --verify && grep -q "generated by git-cliff" CHANGELOG.md && echo "PASS"</automated>
  </verify>
  <done>git-cliff configuration verified and working correctly</done>
</task>

<task type="auto">
  <name>Task 2: Verify release.yml git-cliff integration</name>
  <files>.github/workflows/release.yml</files>
  <action>
Verify .github/workflows/release.yml correctly integrates git-cliff:

1. Check that the workflow:
   - Installs git-cliff via taiki-e/install-action@git-cliff
   - Determines previous tag for changelog generation
   - Runs git-cliff with --config cliff.toml --strip all
   - Pipes output to /tmp/RELEASE_NOTES.md
   - Passes --release-notes to goreleaser

2. Key sections to verify exist:
   ```bash
   grep -A2 "Install git-cliff" .github/workflows/release.yml
   grep "git-cliff --config cliff.toml" .github/workflows/release.yml
   grep "RELEASE_NOTES.md" .github/workflows/release.yml
   ```

3. Verify goreleaser is called with --release-notes:
   ```bash
   grep "release-notes" .github/workflows/release.yml
   ```
</action>
  <read_first>.github/workflows/release.yml</read_first>
  <acceptance_criteria>
    - release.yml contains "taiki-e/install-action@git-cliff"
    - release.yml contains "git-cliff --config cliff.toml --strip all"
    - release.yml contains "RELEASE_NOTES.md"
    - release.yml contains "goreleaser" with "--release-notes"
  </acceptance_criteria>
  <verify>
    <automated>grep -q "taiki-e/install-action@git-cliff" .github/workflows/release.yml && grep -q "git-cliff --config cliff.toml" .github/workflows/release.yml && grep -q "release-notes" .github/workflows/release.yml && echo "PASS"</automated>
  </verify>
  <done>Release workflow git-cliff integration verified</done>
</task>

</tasks>

<verification>
- git-cliff --verify passes
- CHANGELOG.md exists with correct git-cliff footer
- release.yml has all required git-cliff integration points
</verification>

<success_criteria>
- cliff.toml verified with git-cliff --verify
- CHANGELOG.md verified with git-cliff footer
- release.yml verified with all git-cliff integration points
</success_criteria>

<output>
After completion, create `.planning/phases/05-release-infrastructure/05-03-SUMMARY.md`
</output>
